# Flask 기본 설정
SECRET_KEY=dev-secret-key-please-change-in-production
DEBUG=True
HOST=0.0.0.0
PORT=5000

# Admin 설정
ADMIN_PASSWORD=admin123

# JWT 설정 (관리자 인증 토큰)
JWT_SECRET_KEY=jwt-secret-key-please-change-in-production
JWT_ACCESS_TOKEN_EXPIRES=604800  # 토큰 유효기간 (초 단위, 기본값: 7일)

# 프론트엔드 URL (경기 생성 시 URL 반환용 + CORS 허용)
# 로컬 개발: http://localhost:3000
# Vercel 배포: https://your-project.vercel.app (실제 URL로 교체)
FRONTEND_URL=http://localhost:3000

# Redis (캐시)
REDIS_URL=redis://localhost:6379

# MongoDB (팀/멤버 데이터)
MONGO_URI=mongodb://localhost:27017/
MONGO_DB_NAME=nbc_kakaotalk_bot

# PostgreSQL (경기 데이터)
DATABASE_URL=postgresql://localhost:5432/basketball_games